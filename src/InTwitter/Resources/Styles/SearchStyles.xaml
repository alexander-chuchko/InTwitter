<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="InTwitter.Resources.Styles.SearchStyles">

    <Style x:Key="searchEntryStyle" TargetType="Entry">
        <Setter Property="TextColor"
                Value="{DynamicResource Black}"/>
        <Setter Property="PlaceholderColor"
                Value="{DynamicResource DarkGray}"/>
        <Setter Property="FontSize"
                Value="{DynamicResource SmallFontSize}"/>
        <Setter Property="FontFamily"
                Value="Ubuntu400"/>
        <Setter Property="BackgroundColor"
                Value="{DynamicResource LightGray}"/>
        <Setter Property="HorizontalOptions"
                Value="FillAndExpand"/>
        <Setter Property="VerticalOptions"
                Value="FillAndExpand"/>
    </Style>

    <Style x:Key="searchSeparatorStyle" TargetType="BoxView">
        <Setter Property="Color"
                Value="{DynamicResource LightGray}"/>
        <Setter Property="HeightRequest"
                Value="1"/>
        <Setter Property="HorizontalOptions"
                Value="FillAndExpand"/>
    </Style>

</ResourceDictionary>